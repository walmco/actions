# Minimal OpenWrt .config for HiWiFi HC5962 (ramips/mt7621)
# 仅选择目标和设备，保留最小核心包，便于后续在固件中安装其它插件

# Target
CONFIG_TARGET_ramips=y
CONFIG_TARGET_ramips_mt7621=y
CONFIG_TARGET_IMAGES=y

# Device (来自 lean 源码里 mt7621.mk 中的 define Device/hiwifi_hc5962)
CONFIG_TARGET_ramips_mt7621_DEVICE_hiwifi_hc5962=y

#
# Minimal core packages (only essentials)
# 这些是 OpenWrt 运行所需的最小核心包（可以按需删减）
CONFIG_PACKAGE_base-files=y
CONFIG_PACKAGE_busybox=y
CONFIG_PACKAGE_opkg=y
CONFIG_PACKAGE_ubus=y
CONFIG_PACKAGE_uci=y

# 网络与基本服务（保留最小 DHCP/DNS/防火墙 支撑）
CONFIG_PACKAGE_dnsmasq_full=y
CONFIG_PACKAGE_odhcpd-ipv6only=y
CONFIG_PACKAGE_firewall=y

# SSH（远程管理）
CONFIG_PACKAGE_dropbear=y

# 兼容性：保留内核模块自动处理（不要重复声明 DEVICE_PACKAGES 中已有的模块）
# 注：mt7621.mk 中已经写了 DEVICE_PACKAGES := kmod-mt7603 kmod-mt76x2 kmod-usb3
#      所以在此不必手动再添加 kmod-*，以避免重复或产生不必要条目。

# Build options（保留默认压缩/镜像生成）
CONFIG_V=s
# 你可以根据需要开启下面选项
# CONFIG_BUILD_PATENTED=n
# CONFIG_TARGET_KERNEL_PARTSIZE=4096

# End of file
